{
  "name": "habbo-avatar-editor",
  "version": "1.0.0",
  "description": "Habbo Avatar Editor - TypeScript + HTML Canvas port of HabboRegistration.swf v0.916",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/prepare-build.mjs && tsc --noEmit && vite build && node scripts/build-asset-bundle.mjs",
    "preview": "vite preview",
    "serve": "serve demo -l 4173"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "serve": "^14.2.5",
    "typescript": "^6.0.0-beta",
    "vite": "^7.3.1"
  }
}
